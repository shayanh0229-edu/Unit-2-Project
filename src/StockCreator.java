public class StockCreator {
    private String stockName;
    private int stockMarketCap;
    private int stockShares;


    public StockCreator(String stockTicker, int MarketCap, int quantityShares) {
        stockTicker = stockName;
        stockMarketCap = MarketCap; // lines 9 and 10; the variables must be swapped
        stockShares = quantityShares;
    }

    public void stockFluctuations(stockTicker) { //Need to call a stock object using this method

    }
}
